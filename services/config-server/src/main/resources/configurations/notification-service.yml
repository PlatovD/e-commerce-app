spring:
  mongodb:
    username: dima
    password: dima
    host: localhost
    port: 27017
    database: customer
    authentication-database: admin
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.type.mapping: OrderConformation:io.github.platovd.ecommerce.kafka.order.OrderConformation,PaymentNotificationRequest:io.github.platovd.ecommerce.kafka.payment.PaymentConformation
      group-id: orderGroup, paymentGroup
      auto-offset-reset: earliest
  mail:
    host: localhost
    port: 1025
    username: dima
    password: dima
    properties:
      mail:
        smtp:
          trust: '*'
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
server:
  port: 8040